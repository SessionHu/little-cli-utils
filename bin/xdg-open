#!/bin/bash
if grep -qE '^file://' <<< "$1"; then
  exec thunar $@ &
elif grep -qE '^https?://' <<< "$1"; then
  exec chromium-browser $@ &
elif grep -qE '^magnet:' <<< "$1"; then
  exec qbittorrent $@ &
fi

exec termux-open $@
